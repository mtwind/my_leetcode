class Solution:
    def minIncrementForUnique(self, nums: List[int]) -> int:
        # put elements in a map
        # loop through each element, if it is already in the map, increment until it is not anymore, count move
        moves = 0
        numsUsed = {}
        nums.sort()

        # create map with a count of each number

        # after the count, each time a number already exists, increment by 
        
        return moves
    
    
    
